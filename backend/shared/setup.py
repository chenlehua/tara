"""Setup file for editable installs (PEP 660 compatibility)."""

from setuptools import setup

# This file is needed for editable installs with older pip versions
# All configuration is in pyproject.toml
setup()
